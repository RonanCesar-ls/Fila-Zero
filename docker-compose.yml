version: '3.8'

services:
  postgres-db:
    image: postgres:16-alpine
    container_name: filazero-db
    environment:
      POSTGRES_DB: filazero_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
    ports:
      - "5432:5432"